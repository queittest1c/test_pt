

Процедура ПередЗаписью(Отказ)
	
	ФИО = ОбщийМодульКлиентСервер.АналогСтрРазделить(Наименование, " ");
	
	Если ФИО.Количество() > 0 Тогда
		Фамилия = ФИО[0];
	КонецЕсли;
	
	Если ФИО.Количество() > 1 Тогда
		Имя = ФИО[1];
	КонецЕсли;
	
	Если ФИО.Количество() > 2 Тогда
		Отчество = ФИО[2];
	КонецЕсли;
	
КонецПроцедуры

